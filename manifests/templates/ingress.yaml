---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootp                  # Ingress name
  labels:
    app: springbootp
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootp.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootp               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
